{% extends 'base.html.twig' %}

{% block title %}Deal{% endblock %}

{% block body %}
    <h1>Deal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Deal_createdat</th>
                <td>{{ deal.dealCreatedat ? deal.dealCreatedat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Deal_updatedat</th>
                <td>{{ deal.dealUpdatedat ? deal.dealUpdatedat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Deal_content</th>
                <td>{{ deal.dealContent }}</td>
            </tr>
        </tbody>
    </table>


            <a href="{{ path('app_deal_edit', {'id': deal.id}) }}">edit</a>
            {{ include('material/_delete_form.html.twig') }}

{% endblock %}

<form method="post" action="{{ path('app_association_delete', {'id': association.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
{# <div>
    <a href="{{ path('app_association_delete', {'id': association.id}) }}" class="inline-block rounded-md border border-transparent py-2 px-4 text-base font-medium text-white hover:bg-opacity-75" style="background-color: #fb4e17;align-self: center;">Supprimer</a>
</div> #}
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ association.id) }}">
    <button type="submit" class="inline-block rounded-md border border-transparent py-2 px-4 text-base font-medium text-white hover:bg-opacity-75" style="background-color: #fb4e17;align-self: center;">Supprimer l'association</button>
</form>
